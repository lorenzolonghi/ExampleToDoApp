# HOW TO RUN
- After cloning, install the required dependencies with "$ npm install" (you can see the dependencies in the package.json file)
- Import the database schema "./data/database.sql" on you local MySql service
- Insert the infos to the local database in the ./data/database.js" file
- Run the "node index.js" command to make the application start on localhost:3000
